import React from "react";
import style from "./style.module.css";

export default function Nav(props) {
  const { weather, locationString } = props;

  return <div className={style.navContainer}>hi</div>;
}
